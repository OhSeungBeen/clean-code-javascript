// 값식문

// 값이 들어오는 자리에 문이 들어올 수 없다.
// if(for()) {

// }

const numbers = [1, 2, 3, 4];

// 고차 함수 사용
// 식 => 값
if (numbers.map((number) => {}) === 1) {
}

// IIFE (즉시 심행되는 함수 표현식)
// 정의와 동시에 즉시 실행되는 함수
if (
  () => {
    return 'hello';
  }
) {
}
